<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/listeningRoot"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#CC05050A">

    <!-- Tap outside the orb to dismiss -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:orientation="vertical"
        android:gravity="center"
        android:clipChildren="false">

        <!-- speech bubble: state text -->
        <TextView
            android:id="@+id/listeningState"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginBottom="26dp"
            android:background="@drawable/bg_status_badge"
            android:paddingStart="20dp"
            android:paddingEnd="20dp"
            android:paddingTop="11dp"
            android:paddingBottom="11dp"
            android:text="@string/listen_listening"
            android:textColor="@color/accent_light"
            android:textSize="15sp"
            android:textStyle="bold" />

        <!-- the orb -->
        <com.toco.ai.ui.widget.OrbView
            android:id="@+id/listeningOrb"
            android:layout_width="200dp"
            android:layout_height="200dp"
            android:clickable="true"
            android:focusable="true" />

        <!-- live transcript of what was heard -->
        <TextView
            android:id="@+id/listeningHeard"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:paddingStart="40dp"
            android:paddingEnd="40dp"
            android:gravity="center"
            android:minHeight="48dp"
            android:textColor="@color/text_primary"
            android:textSize="17sp" />
    </LinearLayout>

    <!-- close affordance -->
    <TextView
        android:id="@+id/listeningClose"
        android:layout_width="44dp"
        android:layout_height="44dp"
        android:layout_gravity="top|end"
        android:layout_marginTop="24dp"
        android:layout_marginEnd="20dp"
        android:gravity="center"
        android:text="@string/overlay_close"
        android:textColor="@color/text_secondary"
        android:textSize="20sp"
        android:clickable="true"
        android:focusable="true" />
</FrameLayout>
